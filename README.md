# Séa Tanaka portfolio

- `index.html` keeps the original landing page with the smaller centered image.
- `work.html` is a separate work page.
- The work page combines an information/CV-style left column with a recent-projects right column.
- Edit text and links directly in the HTML files.
- Replace the JPG files while keeping their filenames to swap project images.
